<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions.
         Package/scheme alignment: applicationId com.glassdoorsstudio.burgonomics.partner
         matches capacitor.config.ts appId; androidScheme https (no custom scheme:
         no deep-link intent-filter ships — add one with the dispatch-tracking
         feature, not before).
         - POST_NOTIFICATIONS: required on Android 13+ for the FCM kitchen-order
           pushes driven by shared/platform/pushNotifications.ts. Runtime prompt
           fires on first requestPushPermissions(); deny = no pushes, app works.
         - COARSE/FINE_LOCATION: declared for the @capacitor/geolocation plugin
           (capacitor.config.ts). No src callers ship yet (loop-47), so the OS
           never prompts until dispatch-tracking lands.
         - google-services.json: NOT in repo (secret). Place it at
           android/app/google-services.json before release builds; without it the
           gradle google-services plugin is skipped (see android/app/build.gradle)
           and pushes register locally only. -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
</manifest>
